using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TMPro;
using System;

public class RecomendedSurface : MonoBehaviour
{
    public TextMeshProUGUI text;

    // Start is called before the first frame update
    void Start()
    {
        //relacja z rooom surface

    }

    // Update is called once per frame
    void Update()
    {
        //wymiczenie optymalnej potrzebnej powierzchni - 0.35%
    }
}
